# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this
repository.

The project map lives in @AGENT.md — read it first, especially the "Current known
inconsistencies" section, **before** touching any root-level L0/L1/L2 script: several of them
are currently broken against the refactored `simulation/` engine, and that section tracks which.
`simulation/` is a sub-project with its own `CLAUDE.md`/`AGENT.md`/`CONVENTION.md` — read those
when working inside it.

## Contributing rules

Every code change, file rename, and new file MUST follow @CONVENTION.md: commit style, branch
naming, issue labels.

@AGENT.md

@CONVENTION.md
